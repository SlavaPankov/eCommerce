export enum ERoutes {
  main = '/',
  login = '/login',
  registration = '/registration',
  person = '/person',
  catalog = '/catalog',
  category = '/catalog/:id',
  product = '/product/:id',
  cart = '/cart',
  search = '/search',
  all = '*'
}
