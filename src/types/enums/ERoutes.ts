export enum ERoutes {
  main = '/',
  login = '/login',
  registration = '/registration',
  person = '/user',
  catalog = '/catalog',
  category = '/catalog/:id',
  product = '/product/:id',
  cart = '/cart',
  search = '/search',
  all = '*'
}
